# Python Development Rules
- 常に `src/` フォルダ構成を維持し、`old_src/` との直接統合は禁止。
- ファイルを保存または作成した後は、必ず `mypy [file_path]` を実行して型整合性を確認すること。
- 新しい関数を実装した際は、必ず `tests/` 内に対応するテストを作成し、`pytest` で検証すること。
- テストとmypyがパスしていないコードを「完成」として提示してはいけない。
- コミットメッセージは日本語で出力する。